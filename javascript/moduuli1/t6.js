const answer = confirm('Should i calculate the square root')
    if( answer = true);
        num = prompt('syötä numero');
        sqr = num / num
        alert(sqr)
    else{
        alert('the square will not be calculated')
}